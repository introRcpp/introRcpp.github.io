ce passage a disparu du chapitre "packages"

Pour que la compilation du package soit possible, il faut inclure dans `src/` un fichier `Makevars` contenant
```
PKG_CXXFLAGS = -std=c++11
```

+ toute la compilation multifichier
